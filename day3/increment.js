// increment

let counter = 0;
counter++;
counter += 1;

//
function increment(number) {
  return (number + 1);
}

function increment(number) {
  let result = number + 1;
  return result;
}
//
let counter = 0;
counter = increment(counter);
counter = increment(counter);
counter = increment(counter);
counter = increment(counter);
counter = increment(counter);
counter = increment(counter);
counter = increment(counter);
